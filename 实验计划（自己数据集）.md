# 自有电子鼻数据集适配 GNN 传感器漂移校准模型 —— 实验计划

## 一、背景与目标

当前项目在 UCI 公开数据集（16传感器×8特征，6分类，batch1→batch2~10）上验证了物理先验引导的 GNN 域适应方法对传感器漂移的补偿效果。现需将同一套方法适配到自有电子鼻采集的原始数据上，验证方法在实际传感器漂移场景下的有效性。

### 数据概览

| 维度 | 详情 |
|------|------|
| 机器 | 3台（编号1、2、4），其中2台旧版（37个气体传感器）、1台新版（49个气体传感器） |
| 时间跨度 | 5个月，每月1次校准 |
| 气体 | 9种（丙酮、乙醇、乙酸乙酯、正丁醇、甲醛、苯甲醛、氨、异丁醇、甲苯） |
| 浓度 | 3档（30、50、100 ppm）× 3次重复 |
| 采样率 | 1 Hz |
| 实验阶段 | P13基线(3min) → P15/P17进样(5min) → P16/P18清洗(10min) |
| 数据格式 | `.txt` 空格分隔，无表头，每行1个时间采样点 |

### 传感器列号排布

详见 `Electronic_Nose_Data_Format.md`：

- **旧版（37个气体传感器）**：第13-49列（索引12-48），共37个
- **新版（49个气体传感器）**：分5段碎片化分布
  - 第13-49列（索引12-48）：基础37个
  - 第57-60列（索引56-59）：扩展组1（4个）
  - 第70-72列（索引69-71）：扩展组2（3个）
  - 第77-79列（索引76-78）：光学/红外类（3个）
  - 第82-83列（索引81-82）：酒精传感器（2个）
- 跨机器对齐时，取新旧版共有的37个传感器（索引12-48）

---

## 二、核心任务定义

延续现有代码框架，任务设定为 **9类气体分类**（现有代码6分类 → 扩展到9分类）：

- **源域（Source Domain）**：第1个月的数据（传感器状态最新，漂移最小，作为"干净"参考）
- **目标域（Target Domain）**：第2~5个月的数据（漂移逐渐增大）
- **范式**：源域（全量标签）→ 目标域（无标签/少量标签）的无监督/半监督域适应

这直接对应现有 UCI 数据集的 batch1 → batch2~10 实验范式。

---

## 三、数据预处理管道

需要新建 `custom_dataset_loader.py`，实现从原始 `.txt` 到 PyG 图数据的完整管道。

### 3.1 原始数据读取

- 1个实验文件 = 1种气体 × 1个浓度 × 1次重复
- 每文件约 1080 行（18分钟 × 60秒 × 1Hz）
- 解析 `.txt` 空格分隔文件，无表头
- 提取 Stage 列（第5列，索引4）用于阶段分割
- 提取目标气体传感器列（按机器版本选择对应的列索引范围）
- 采样率1Hz，无需降采样

### 3.2 文件组织假设

```
Raw_Data/
├── Machine_1/                  # 旧版机器1
│   ├── Month_1/
│   │   ├── acetone_30ppm_rep1.txt
│   │   ├── acetone_30ppm_rep2.txt
│   │   ├── ...
│   │   └── toluene_100ppm_rep3.txt   # 9×3×3=81文件/月
│   ├── Month_2/
│   ├── Month_3/
│   ├── Month_4/
│   └── Month_5/
├── Machine_2/                  # 旧版机器2
│   └── ...
└── Machine_4/                  # 新版机器
    └── ...
```

（实际文件组织结构待确认后调整）

### 3.3 阶段分割

根据 Stage 列（索引4）的值对每个文件的时间序列进行分割：

| Stage | 含义 | 时长 | 用途 |
|-------|------|------|------|
| P13 | 主基线阶段 | 3 min (180点) | 计算传感器基线值 R₀ |
| P14 | 备用基线阶段 | — | P13不稳定时的替代基线 |
| P15 | 主进样阶段 | 5 min (300点) | 计算响应幅度、动态特征 |
| P17 | 扩展进样阶段 | — | 双气室系统的第2进样口（可选） |
| P16 | 主清洗阶段 | 10 min (600点) | 提取恢复特征 |
| P18 | 扩展清洗阶段 | 10 min | 清洗P17通道（可选） |

### 3.4 特征提取（每个传感器）

每个实验文件 → 1个样本（图）。对每个传感器，从时间序列中提取特征。

**初版建议提取 8 维特征**（匹配现有模型输入维度）：

| # | 特征名 | 计算公式 | 说明 |
|---|--------|---------|------|
| 1 | 基线均值 | mean(P13) | 传感器在洁净空气中的基准电阻 |
| 2 | 基线标准差 | std(P13) | 基线稳定性 |
| 3 | 响应幅度 ΔR | max(P15) - mean(P13) | 经典响应强度 |
| 4 | 归一化响应 | ΔR / mean(P13) | 消除传感器个体差异 |
| 5 | 稳态响应值 | mean(P15最后30秒) | 吸附平衡后的平台值 |
| 6 | 最大上升斜率 | max(diff(P15)) | 响应速度 |
| 7 | 恢复率 | mean(P18最后30秒) / mean(P13) | 脱附程度（理想值≈1.0） |
| 8 | 响应AUC | sum(P15 - mean(P13)) | 累积响应量（积分） |

**后续可扩展到 12~16 维**，增加：到达90%最大值的时间、半恢复时间、P15内标准差、P15降采样多尺度均值等。

### 3.5 数据集规模

| 维度 | 计算 | 数量 |
|------|------|------|
| 每月/每机器 | 9气体 × 3浓度 × 3重复 | 81 样本 |
| 总样本量 | 81 × 5月 × 3机器 | 1215 样本 |
| 每样本节点数（旧版） | 37 气体传感器 | 37 节点 |
| 每样本节点数（新版） | 49 气体传感器 | 49 节点 |

### 3.6 图构建

- **节点**：每个气体传感器 = 1个节点
- **节点特征**：N维提取特征 + sensor_id one-hot（37或49维）
- **边**：全连接或基于物理先验 Rs 稀疏化
- **边权重 Rs**：从源域数据计算传感器间 Pearson 相关系数矩阵，阈值过滤（>0.3）

```python
# 示例接口
def load_custom_dataset(data_dir, machine_id, month, scaler=None, sensor_config='old'):
    """
    加载自定义数据集，返回 X(numpy), y, scaler
    sensor_config: 'old' (37 sensors) / 'new' (49 sensors) / 'common' (37 shared)
    """
    ...

def build_physical_adjacency_custom(X_source, sensor_ids, threshold=0.3):
    """计算传感器间物理先验矩阵 Rs"""
    ...

def convert_to_pyg_graphs_custom(X, y, R_s_matrix, num_sensors, domain_id):
    """转换为 PyG Data 对象列表"""
    ...
```

---

## 四、实验场景设计

### 场景 A：单机逐月漂移（核心实验）⭐

最直接对应现有 batch1→batch2~10 的设置。

| 源域 | 目标域 | 类比 |
|------|--------|------|
| 机器i 第1月 | 机器i 第2月 | batch1→batch2（短期漂移） |
| 机器i 第1月 | 机器i 第3月 | batch1→batch5（中期漂移） |
| 机器i 第1月 | 机器i 第4月 | batch1→batch7（中长期漂移） |
| 机器i 第1月 | 机器i 第5月 | batch1→batch10（长期漂移） |

- 对3台机器分别独立运行
- 每台机器：4个 target month × 7配置 = 28次训练
- 总计：3 × 28 = 84次训练

**可回答的问题**：
- 新版机器（49传感器）比旧版（37传感器）漂移更严重还是更轻？
- 多传感器信息能否提升漂移鲁棒性？
- 各机器的漂移速率是否一致？

### 场景 B：跨机器泛化（迁移实验）

利用3台机器传感器重叠（37个公共传感器），测试模型在不同机器间的迁移能力。

| 源域 | 目标域 | 说明 |
|------|--------|------|
| 旧机器1 第1月 | 旧机器2 第1月 | 同期跨机器泛化 |
| 旧机器1 第1月 | 新版机器 第1月 | 跨版本泛化（仅37公共传感器） |
| 旧机器1 第1月 | 旧机器2 第2~5月 | 跨机器+跨时间漂移 |
| 旧机器1 第1月 | 新版机器 第2~5月 | 最困难：跨版本+跨时间 |

### 场景 C：多源域合并（增大源域）

| 源域 | 目标域 |
|------|--------|
| 3台机器 第1月合并 | 每台机器 第2~5月 |
| 旧机器1+2 第1月合并 | 新版机器 第2~5月 |

### 场景 D：浓度泛化（附加实验）

| 源域 | 目标域 | 说明 |
|------|--------|------|
| 30, 100 ppm | 50 ppm | 从两端推断中间浓度 |
| 30, 50 ppm | 100 ppm | 从低浓度推断高浓度 |

---

## 五、实验配置矩阵

沿用已优化的7个配置（来自 `run_all_batches.py` 最终版）：

| # | 配置 | JDA权重 | EMA | 多尺度 | 半监督 | 距离度量 |
|---|------|---------|-----|--------|--------|----------|
| 1 | Pure Self-Attention GNN | 0.0 | No | No | No | — |
| 2 | GNN + JDA (MK-MMD) | 0.05 | No | No | No | MMD |
| 3 | UDA + EMA (MMD) | 0.05 | Yes | No | No | MMD |
| 4 | Multi-Scale + MMD（**推荐UDA**） | 0.10 | Yes | Yes | No | MMD |
| 5 | SSDA 12 labels (MMD) | 0.10 | Yes | Yes | Yes | MMD |
| 6 | Multi-Scale + Wasserstein | 0.20 | Yes | Yes | No | Sinkhorn |
| 7 | SSDA 12 labels (Wasserstein) | 0.20 | Yes | Yes | Yes | Sinkhorn |

加上4个传统基线：**SVM、PCA+SVM、LDA、MLP**。

关键超参数（来自调优结论）：
- JDA权重：0.10（MMD） / 0.20（Wasserstein）
- mid_weight：0.7（深层语义对齐占70%，浅层占30%）
- Warm-up：epoch 5→15 线性增加
- 学习率：0.005，CosineAnnealingLR(T_max=50)
- EMA：V2动态阈值退火（0.95→0.50）+ 置信度加权

---

## 六、预期产出

1. **漂移曲线**：各机器精度随月份衰减曲线（类似现有 Batch2~10 表格），可视化传感器漂移的时间演化
2. **三台机器对比**：漂移速率、校准难度、传感器数量对鲁棒性的影响
3. **新版 vs 旧版**：49传感器相比37传感器的优势和劣势
4. **消融分析**：物理先验 Rs、多尺度对齐、EMA 在真实数据上的增益是否与公开数据集一致
5. **SSDA标签效率**：12个目标域标签能达到多高精度，为实际部署提供标注成本参考

### 结果表格示例

```
场景A — 机器1（旧版37传感器）
─────────────────────────────────────────────────
配置              M1→M2   M1→M3   M1→M4   M1→M5   Avg
─────────────────────────────────────────────────
Pure GNN           xx.x    xx.x    xx.x    xx.x    xx.x
GNN + JDA          xx.x    xx.x    xx.x    xx.x    xx.x
Multi-Scale + MMD  xx.x    xx.x    xx.x    xx.x    xx.x
SSDA 12 labels     xx.x    xx.x    xx.x    xx.x    xx.x
─────────────────────────────────────────────────
```

---

## 七、实施步骤

| 步骤 | 内容 | 涉及文件 |
|------|------|----------|
| **Step 1** | 确认原始数据文件组织结构和命名规则 | — |
| **Step 2** | 编写 `custom_dataset_loader.py`：读取.txt → 阶段分割 → 特征提取 → Z-score标准化 → 图构建 | 新建 |
| **Step 3** | 数据探索与验证：可视化典型传感器响应曲线、检查特征分布、确认阶段标签正确 | — |
| **Step 4** | 适配 `model.py`：将 `num_classes` 从6改为9，`num_sensors` 从16改为动态参数 | 修改 |
| **Step 5** | 编写 `batch_runner_custom.py`：调用自定义数据集，运行场景A实验 | 新建（基于 `batch_runner.py`） |
| **Step 6** | 运行场景A全量实验（3机器 × 4 target month × 7配置） | — |
| **Step 7** | 运行场景B/C/D对照实验 | — |
| **Step 8** | 结果汇总、可视化、与公开数据集结论对比分析 | — |

---

## 八、待确认事项

1. **特征提取维度**：初版8维是否合适？是否需要在初版就加入更多特征？
2. **跨机器传感器对齐**：是否统一使用37个公共传感器（简化模型），还是每台机器独立建模（充分利用新版传感器）？
3. **浓度处理**：3个浓度合并训练（增大样本量81→81样本/月/机，但增加类内方差），还是固定单个浓度独立训练（27样本/月/机）？
4. **分类目标**：9类气体分类，还是 9×3=27类细粒度分类（气体×浓度）？后者能同时验证分类+浓度辨识能力。
5. **文件组织结构**：请确认原始数据实际的文件目录结构，便于编写数据加载器。
