# 5.28 优化方向讨论总结

## 当前状态

### UCI 公开数据集（16传感器 × 6气体，Batch 1→2~10）

| 方法 | 平均准确率 | 备注 |
|------|-----------|------|
| Pure GNN | 60.2% | 无任何域适应 |
| Multi-Scale MMD (UDA) | 60.3% | 最佳无监督方案 |
| Wasserstein (UDA) | 50.8% | 不如 MMD |
| SSDA 12 labels (MMD) | 84.8% | 半监督上界 |
| SSDA 12 labels (Wasserstein) | 85.5% | 略优但方差大 |
| AL v2 KM-Margin (60 labels) | 88.0% | 主动学习最佳，但不稳定 |

### 自定义数据集（49传感器 × 9气体，样机4，M1→M2~M5）

| 实验条件 | 最佳 UDA | 最佳 SSDA | 特征模式 |
|----------|---------|----------|----------|
| 全浓度混合 | 55.2% | 55.8% | temporal |
| 100ppm 单一浓度 | 52.1% | 66.9% | temporal |
| 50ppm 单一浓度 | 46.3% | 72.2% | temporal |
| 30ppm 单一浓度 | 34.2% | 57.2% | temporal |
| 手工艺特征（方案A） | 45.2% | — | handcrafted |

**核心矛盾**：UDA 与 SSDA 之间存在 20-25% 的巨大差距，说明当前无监督方法在极端漂移场景下基本失效。

---

## 当前方法的根本局限性分析

### JDA/MMD 的隐含假设

JDA 假设存在一个**与域无关的特征空间**（covariate shift assumption），即 `P(Y|X)` 在不同域间保持不变。当传感器漂移改变了条件分布——同一响应模式在不同月份代表不同气体——这个假设被破坏。

### 三个具体问题

| # | 问题 | 严重程度 |
|---|------|---------|
| 1 | 伪标签循环论证：模型用自己不准的预测去对齐，可能越对齐越错 | 中（EMA+动态阈值部分缓解） |
| 2 | 类别互换：漂移导致两类响应模式交换，边缘MMD无法察觉 | **高（纯UDA无解）** |
| 3 | 极端漂移时 MMD 梯度消失：源域和目标域分布在 RKHS 中几乎无重叠 | 高（b8/b10 反复崩坏） |

---

## 优化方向

### 方向 A：增量/渐进域自适应（Gradual Domain Adaptation）⭐⭐⭐⭐⭐

**思路**：利用数据天然的时间顺序，逐步适应而非一步到位。

```
当前：M1(源) → M5(目标)  一步跳4个月，漂移量太大
改进：M1(源) → M2(目标) → M1+M2(新源) → M3(目标) → ... → M5
```

**优点**：
- 每步漂移量小，伪标签准确率更高
- 源域覆盖的月份变化越来越多，模型逐渐学到"月份无关"的表示
- 实现难度低，改造现有代码即可

**风险**：
- 误差累积：早期错误会传播到后续月份
- 缓解策略：保留一部分 M1 数据始终在源域中（避免灾难性遗忘）

**预期收益**：中高 | **实现难度**：低

---

### 方向 B：预训练 + 解冻微调（Two-Stage Training）⭐⭐⭐⭐

**思路**：将训练拆为两个阶段，避免 JDA 在模型未收敛时产生噪声梯度。

```
阶段1：源域充分训练（50+ epochs），不做法对齐
阶段2：冻结 GNN 底层，仅微调顶层 + 分类器 + 域对齐层
```

**为什么当前 5 epoch 热身不够**：
- 5 epoch 后模型在源域上的分类边界仍不稳定
- JDA 从 epoch 6 开始介入时，源域嵌入空间还在大幅变化
- 两阶段训练让 JDA 在"稳定锚点"上工作

**预期收益**：中 | **实现难度**：低

---

### 方向 C：对抗域自适应（DANN/GRL）⭐⭐⭐⭐

**思路**：不依赖伪标签，通过对抗训练直接让 GNN 产出"与域无关"的嵌入。

```
GNN编码器 → 嵌入 → 分类器（预测气体类别）
              ↓
         域分类器（预测月份）← 梯度反转层（GRL）
```

训练目标：
- 分类器：尽可能准（正常梯度下降）
- 域分类器：尽可能**猜不出月份**（梯度反转 → GNN 被逼着产出"月份无关"的嵌入）

**与 JDA 的关键区别**：
- JDA 是显式距离最小化（"把两个分布拉到一起"）
- DANN 是隐式对抗（"让判别器分不清来自哪个月"）
- DANN **不需要伪标签**，从根本上避开了循环论证问题

**风险**：
- GAN 类训练本身不稳定，需要仔细调参
- 可能学到"trivial solution"（嵌入坍塌到一点）

**预期收益**：中高 | **实现难度**：中

---

### 方向 D：原型对齐（Prototype Alignment）⭐⭐⭐

**思路**：用源域各类别的原型向量（均值嵌入）替代伪标签，避免循环论证。

```
源域：计算各类别的原型 c_k = mean(embedding of class k in source)
目标域：每个样本向最近的原型靠拢

损失 = 分类损失(源域) + 目标域样本到最近原型的距离
```

**与 JDA 伪标签的关键区别**：
- 原型是用源域**真实标签**算的，不受目标域预测质量影响
- 不需要对目标域"猜类别"，只需要"靠近某个已知的原型"

**局限**：
- 原型在极端漂移下也可能失效（M1 的乙醇原型可能不代表 M5 的乙醇）
- 需要类别在嵌入空间中形成可区分的簇

**预期收益**：中 | **实现难度**：中

---

### 方向 E：时间条件编码（Temporal Conditioning）⭐⭐⭐⭐

**思路**：把"月份信息"显式地喂给模型，让模型学会"根据月份调整预测"，而不是强行消除月份差异。

```python
# 当前做法：抹掉月份信息
JDA: MMD(M1嵌入, M5嵌入) → 0  # 强制分布一致

# 替代做法：利用月份信息
month_embedding = Embedding(month_id)  # 可学习的月份表示
联合输入: GNN(sensor_data, month_embedding) → 预测
```

**直觉**：传感器漂移是客观存在的物理现象。与其假装它不存在（JDA的做法），不如让模型知道"这是第5个月的数据"，然后学会补偿。

**你的原话**："对齐的意义是强行把现在的数据拉回到原来，去适配原来的模型，本身我感觉就不符合直觉，我反而觉得去让模型适配新数据也许是一条路"

**实现路径**：
1. 所有月份数据混在一起训练，加入月份 ID 作为额外特征
2. 测试时：给定新月份的数据 + 新月份 ID，模型自动调整
3. 甚至可以训练一个"漂移预测"子网络，预测传感器响应在不同月份间的变化

**风险**：
- 月份 ID 可能被忽略（模型学不会利用它）
- 新月份（M6）的 ID 在训练中未见过，需要泛化

**预期收益**：中高 | **实现难度**：中

---

### 方向 F：对比自监督预训练（Contrastive Pre-training）⭐⭐⭐

**思路**：在所有月份数据上做对比学习，学到"月份无关的传感器表示"，再微调分类。

```python
# 正样本对：同一气体在不同月份的两个样本
# 负样本对：不同气体的样本
loss = InfoNCE(embedding_i, embedding_j)
```

**优点**：完全不需要标签，可以充分利用所有月份的无标签数据

**局限**：
- 需要较大数据量（当前每个单浓度月份仅~30个样本，严重不够）
- 混合浓度场景（~270样本/月）勉强可用
- 数据增强策略在传感器数据上不直观

**预期收益**：低（数据量不足） | **实现难度**：高

---

### 方向 G：Test-Time Adaptation (TTA) ⭐⭐⭐⭐⭐

**思路**：推理时遇到新月份数据，当场用无监督信号微调几个 batch。

```
部署流程：
1. 用 M1 训练基础模型
2. M2 数据到达 → 用 M2 的无标签数据微调模型（例如熵最小化）
3. 微调后的模型在 M2 上推理
4. M3 数据到达 → 在 M2 模型基础上继续微调
```

**与增量学习的区别**：
- 增量学习：重新训练（或大量微调），离线完成
- TTA：推理时在线微调，轻量级（通常只更新 BN 层或几个参数）

**优点**：
- 完全不需要目标域标签
- 每次只适应一个小漂移
- 模型随月份自然进化

**局限**：
- 无监督信号的选择（熵最小化？伪标签？）仍然是核心挑战
- 如果连续多个月不做真实标签校准，误差会累积

**预期收益**：高 | **实现难度**：中高

---

### 方向 H：放弃无监督，拥抱最小代价标签 ⭐⭐⭐⭐⭐

**现实考量**：

| 场景 | 所需标签 | 准确率 | 标签成本 |
|------|---------|--------|----------|
| UDA (Multi-Scale MMD) | 0 | 46-55% | 零 |
| SSDA 1 label/class | 9 | 57-72% | 极低（每种气体测1次） |
| SSDA 2 labels/class | 18 | — | 很低 |
| AL + SSDA (KM-Margin) | 60 | 88% | 中等 |
| 全监督 | 全部 | ~95%+ | 高 |

**关键信息**：SSDA 用**每类 1 个样本**就在 50ppm 场景下从 46.3% → 72.2%，提升了 25.9%。这说明：

- 在真实部署中，每种气体每月仅需采集 1 次带标签样本，就能让模型保持可用
- 这个成本在工业场景中几乎可以忽略（每种气体测一次，几分钟完成）
- "完全无监督"是学术问题，"最小代价标签"是工程问题——后者更值得解决

---

## 关于 Rs 物理邻接矩阵的重新评估

### Rs 是否多余？

GATv2Conv 的注意力机制本身就能从数据中学习边权重。你提出的疑问是对的。

**Rs 的实际作用**：
1. 在**数据极少**时提供初始化先验（源域和目标域共享同一个图结构）
2. Rs 从源域计算，作为"不会漂移的结构锚点"
3. 但从实验结果看（Config 1 Pure GNN vs Config 2 GNN+JDA），Rs 的边际贡献有限

**建议**：保留 Rs 作为默认（零成本保险），但不投入更多精力优化它。GAT 注意力机制足以胜任边权重的学习。

---

## 多尺度对齐的正确理解

**对齐的不是原始数据，而是 GNN 各层输出的嵌入分布**：

```
原始传感器数据（不做任何对齐）
        ↓
  GNN Conv1 → mid_domain (64维) ← 70%权重的MMD对齐
        ↓
  GNN Conv2 → domain_emb (32维) ← 30%权重的MMD对齐
        ↓
  分类器 → 预测
```

中间层离原始传感器信号更近，捕捉的是**局部传感器协同模式**（"传感器1-5一起在响应"），这些模式跨月份更稳定。最终层已被高度抽象，更容易被漂移扭曲。这就是 `mid_weight=0.7` 的原因。

**这不是"对模型对齐"，而是"在模型的不同深度对嵌入空间做域对齐约束"。**

---

## 优先级建议

| 优先级 | 方向 | 理由 |
|--------|------|------|
| **P0** | 方向 E：时间条件编码 | 你的核心直觉"让模型适配数据"，可行性高 |
| **P0** | 方向 A：增量域自适应 | 天然契合时序列数据，实现简单，预期收益高 |
| **P1** | 方向 B：预训练+解冻微调 | 改进现有训练流程，低风险 |
| **P1** | 方向 C：对抗域自适应 | 绕过伪标签问题，中等实现成本 |
| **P2** | 方向 G：Test-Time Adaptation | 长期最有前景但需要更多基础设施 |
| **P2** | 方向 H：最小代价标签 | 工程上最务实，与研究方向并行探索 |
| **P3** | 方向 D：原型对齐 | 伪标签的替代方案，可以先小规模验证 |
| **P3** | 方向 F：对比预训练 | 数据量不足，暂缓 |

---

## 未回答的开放问题

1. **漂移是否可预测？** 传感器老化是否有物理规律（如指数衰减）？如果漂移有可建模的动力学，就可以用 Kalman Filter 或状态空间模型直接预测补偿量，彻底绕过域自适应。

2. **不同气体的漂移速率是否相同？** 如果某些气体（如乙醇）漂移快、另一些（如丙酮）漂移慢，可以为每种气体学习不同的漂移模型。

3. **传感器级别的漂移 vs 系统级别的漂移？** 当前 GNN 把 49 个传感器作为一个整体图来处理。如果漂移主要是传感器个体级别的（如某个传感器灵敏度持续下降），也许在节点特征层面做补偿比在嵌入空间对齐更直接。

---

*讨论日期：2026-05-28*
*基于：UCI 数据集实验九~十一 + 自定义数据集全浓度实验 + GNN/JDA 原理深入讨论*

---

---

# 域泛化与增量适应对比实验计划（UCI 公开数据集）

*最终整理：2026-05-29*

## 实验目标

回答三个核心问题：
1. **静态源域迁移的上限在哪？** 单源域 UDA/SSDA 对每个目标 batch 的表现（已完成基线）
2. **模型能否随月份"进化"？** 增量获取标签 + warm-start 是否能比静态 SSDA 更好
3. **漂移规律是否可学？** 见过前几个 batch 后，模型能否泛化到未见过的 batch

---

## 三条路线总览

```
Batch 时间线:  B1 ──→ B2 ──→ B3 ──→ B4 ──→ B5 ──→ B6 ──→ B7 ──→ B8 ──→ B9 ──→ B10
              (445)  (413)  (355)  (294)  (161)  (246)  (260)  (456)  (380)  (352)
              
路径1 (静态源域):
  B1 ──────────────────────────────────────────→ B_n   对每个 n=2..10 独立
  源域固定为B1，每目标batch独立训练

路径2 (增量进化 SSDA only, warm-start):
  B1 → B2 ──→ B1+B2 → B3 ──→ B1+B2+B3 → B4 ──→ ... → B10
  模型权重继承 + 源域数据累积

路径3 (域泛化 3c):
  B1+B2+B3+B4+B5 联合训练 ──→ 零样本测试 B6~B10
```

---

## 路径 1：静态源域基线 (Baseline)

### 设计

| 参数 | 值 |
|------|-----|
| 源域 | Batch 1（445 样本，全标签） |
| 目标域 | Batch 2~10，每个独立 |
| UDA 分支 | multi_scale_jda_loss, 0 target labels |
| SSDA 分支 | multi_scale_jda_loss, 12 target labels (2/class) |
| 模型初始化 | 随机初始化（每个目标 batch 独立从零训练） |
| Epochs | 50 |
| 超参数 | JDA=0.10, mid_weight=0.7, lr=0.005 |

### 输出

| Batch | UDA Acc | SSDA Acc | SSDA-UDA Gap |
|-------|---------|----------|--------------|
| B2 | | | |
| B3 | | | |
| ... | | | |
| B10 | | | |
| **Avg** | | | |

> 注：此结果已从服务器全批次实验（实验九）获得，直接复用。UDA = Multi-Scale + MMD 行，SSDA = SSDA 12 labels (MMD) 行。

---

## 路径 2：增量进化 (Continual SSDA)

### 核心假设

模型参数从前一个月份继承（warm-start），使每一步的初始化点离目标域更近。源域数据随月份累积，覆盖的漂移范围逐渐扩大。

### 训练流程（逐步进行）

```
初始: 在 B1 上训练基础模型 (全监督, 50 epochs, 无域适应)
      → 保存 checkpoint_0

Step 1 (B1 → B2):
  校准前: checkpoint_0 直接在 B2 上测试 → acc_before_2
  校准后: checkpoint_0 warm-start, SSDA 训练
          源域 = B1(全标签)
          目标域 = B2(12 labeled + 其余 unlabeled)
          → 测试 B2 → acc_after_2
          → 保存 checkpoint_1

Step 2 (→ B3):
  校准前: checkpoint_1 直接在 B3 上测试 → acc_before_3
  校准后: checkpoint_1 warm-start, SSDA 训练
          源域 = B1(全标签) + B2_cal(12 labeled)
          目标域 = B3(12 labeled + 其余 unlabeled)
          → 测试 B3 → acc_after_3
          → 保存 checkpoint_2

...重复至 B10
```

### 关键设计细节

**源域数据累积规则**：
- B1：始终保留全量 445 样本（全标签）
- B2~B_{n-1}：仅保留 **12 个校准标签样本**（每类 2 个）加入源域
- 目标域 B_n：12 个校准标签样本用于 SSDA 监督信号

**"校准前"的定义**：
- 上一步训练完成的模型，**不做任何额外训练**
- 直接在当步目标 batch 全集上测试（zero-shot transfer）
- 衡量模型对"未见过的月份"的泛化能力

**"校准后"的定义**：
- 从 checkpoint_{n-1} 加载权重作为初始化
- 用 SSDA（12 个目标域标签 + 累积源域）完整训练 50 epochs
- 在当前目标 batch 全集上测试

**标签预算**：

| 项目 | 数量 |
|------|------|
| B1 标签 | 445（全量，本身就是监督训练） |
| B2~B10 校准标签 | 9 × 12 = **108** |
| 路径 1 SSDA 标签 | 9 × 12 = **108** |
| **两条路径标签预算完全相等** | |

### 输出

| Step | 目标 | 校准前 Acc | 校准后 Acc | Δ (校准提升) | 路径1 SSDA Acc | 路径2 vs 路径1 |
|------|------|-----------|-----------|-------------|---------------|---------------|
| 1 | B2 | | | | | |
| 2 | B3 | | | | | |
| 3 | B4 | | | | | |
| 4 | B5 | | | | | |
| 5 | B6 | | | | | |
| 6 | B7 | | | | | |
| 7 | B8 | | | | | |
| 8 | B9 | | | | | |
| 9 | B10 | | | | | |
| **Avg** | | | | | | |

**核心观察指标**：
- 校准前 是否随 step 增加而上升？（模型越来越会泛化？）
- 校准后 vs 路径1 SSDA：同等标签预算下，增量策略是否更优？
- Δ(校准提升) 是否逐渐缩小？（模型越来越不需要校准？）

### 随机种子

使用 3 个种子 `[42, 123, 456]`，报告 mean ± std。

### 未做的变体（实验记录中标注）

- **方案 B (From-Scratch)**：每步从零初始化而非 warm-start，仅源域数据累积 → 未做。与方案 A 对比可区分"数据累积的贡献" vs "模型继承的贡献"
- **UDA 分支**：路径 2 的纯无监督版本 → 未做。理由是 5.28 讨论中认定 UDA 在极端漂移下伪标签循环论证不可靠

---

## 路径 3：域泛化 (Domain Generalization, 3c)

### 核心假设

模型在多个源域上联合训练后，能学到跨域不变的特征表示，从而在**未见过的目标域**上直接泛化。

### 训练设置

| 参数 | 值 |
|------|-----|
| 训练集 | Batch 1~5 全部数据（合并为一个大的训练集） |
| 训练方式 | 标准监督学习（无域标签、无域适应损失、无 JDA/MMD） |
| 测试集 | Batch 6~10（零样本，不做任何适应） |
| 对比基线 | 路径 1 UDA 在 B6~B10 的结果（路径 1 只用 B1、且用了 JDA） |

### 为什么只做到 B5 训练？

B1~B5 覆盖了从小漂移（B2 离 B1 近）到大漂移（B5 样本量骤降到 161）的场景。B6~B10 的漂移量级与 B5 之后的变化需要测试泛化能力。

### 输出

| 测试 Batch | 路径3 Acc (多源域联合) | 路径1 UDA Acc (B1 only) | 路径1 SSDA Acc (B1 only + 12 labels) |
|------------|----------------------|------------------------|-------------------------------------|
| B6 | | | |
| B7 | | | |
| B8 | | | |
| B9 | | | |
| B10 | | | |
| **Avg** | | | |

**核心观察**：
- 路径 3 vs 路径 1 UDA：多源域训练能否超越单源域 + JDA 对齐？
- 路径 3 vs 路径 1 SSDA：零样本泛化能否接近 12 标签 SSDA？
- 如果路径 3 > 路径 1 UDA，说明"见过更多域"本身就比"单域+对齐"强

### 随机种子

使用 3 个种子 `[42, 123, 456]`，报告 mean ± std。

### 未做的变体（实验记录中标注）

- **3a (对抗域泛化)**：加入域分类器 + 梯度反转层（GRL），显式逼迫模型学到域不变特征 → 未做
- **3b (元学习 MAML)**：模拟"从某域快速适应到另一域"的元学习过程 → 未做

---

## 完整实验矩阵

| 实验编号 | 路径 | 源域 | 目标域 | 方法 | 标签预算 | 种子数 | 状态 |
|---------|------|------|--------|------|---------|--------|------|
| P1-UDA | 1 | B1 | B2~B10 each | Multi-Scale MMD UDA | 0 | 1 (42) | ✅ 已完成 |
| P1-SSDA | 1 | B1 | B2~B10 each | Multi-Scale MMD SSDA | 108 | 1 (42) | ✅ 已完成 |
| **P2-SSDA** | **2** | **B1→B1+B2→...** | **B2→B3→...→B10 依次** | **Warm-start SSDA** | **108** | **3** | **待做** |
| **P3-DG** | **3** | **B1~B5 联合** | **B6~B10 零样本** | **标准监督学习** | **0 (对测试域)** | **3** | **待做** |

---

## 预期结论空间

| P1 vs P2 | P3 vs P1 UDA | 含义 |
|----------|-------------|------|
| P2 > P1 | — | 增量 warm-start 有效，模型持续进化优于独立训练 |
| P2 ≈ P1 | — | 数据累积和 warm-start 无帮助，SSDA 效果主要来自标签本身 |
| P2 < P1 | — | Warm-start 导致错误累积（灾难性遗忘/确认偏差） |
| — | P3 > P1 UDA | "见多识广"比"单域+对齐"更有效 |
| — | P3 ≈ P1 UDA | 多源域简单联合训练不提供额外泛化能力，需 3a/3b |
| — | P3 < P1 UDA | 域间差异太大，简单混合反而让模型混乱 |

---

## 文件规划

| 文件 | 内容 |
|------|------|
| `run_all_batches.py` | 路径 1 脚本（已完成） |
| `continual_adaptation.py` | 路径 2 脚本（待写） |
| `domain_generalization.py` | 路径 3 脚本（待写） |
| `5.28实验.md` | 本文档（讨论 + 计划） |

---

## 实施顺序

1. **先写 `continual_adaptation.py`**（路径 2）— 核心改动是 warm-start + 校准前/后双报告
2. **再写 `domain_generalization.py`**（路径 3）— 较简单，相当于去掉域适应的标准训练
3. 两条路径在同一台服务器上跑，结果汇总到本文档
